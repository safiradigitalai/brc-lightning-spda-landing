version: '3.8'
services:
  app:
    build: .
    ports:
      - '3000:3000'
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
    command: sh -c "yarn build && yarn start"
